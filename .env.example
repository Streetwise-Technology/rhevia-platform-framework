# Rhevia Platform Framework — Environment Variables
# Copy this file to .env and fill in the values:
#   cp .env.example .env

# --- Required ----------------------------------------------------------------

# Mapbox access token for map visualisations (deck.gl / MapBox GL)
MAPBOX_TOKEN=

# --- Build-time (used by data loaders and promote script) --------------------

# ISO 8601 timestamps defining the report period
PERIOD_START="2026-01-19T00:00:00Z"
PERIOD_END="2026-01-20T00:00:00Z"

# --- BigQuery (used by Python data loaders) -----------------------------------

# GCP project ID for BigQuery dataset and gsutil uploads
BQ_PROJECT_ID=

# Organisation subdomain — determines which parameterised route to build
ORG_SUBDOMAIN=

# --- Deployment ---------------------------------------------------------------
# Base URL path for GitHub Pages (leave unset for local dev and GCS promotion).
# BASE_PATH=/rhevia-platform-framework/

# --- GCP Authentication (for promotion + BigQuery) ---------------------------
# Point to a service account key JSON file (.keys/ is gitignored).
# This authenticates both gsutil and BigQuery without gcloud CLI login.
GOOGLE_APPLICATION_CREDENTIALS=.keys/sa-key.json

# --- GitHub Actions Secrets --------------------------------------------------
# These are NOT set in .env — they go in GitHub repo settings:
#   Settings → Secrets and variables → Actions → New repository secret
#
# GCP_SA_KEY     — Raw JSON content of the GCP service account key
#                  (google-github-actions/auth@v2 writes this to a temp file
#                   and sets GOOGLE_APPLICATION_CREDENTIALS automatically)
# MAPBOX_TOKEN   — Same Mapbox token as above
